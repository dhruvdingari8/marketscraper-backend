# Scrapers

This module contains the code for actually scraping different websites. Each website will have an individual file with methods to scrape listings from its designated site. In order to prevent overloading the websites traffic limits, scraping will not be too intensive, with time limits being expanded to not fewer than a minute or two.